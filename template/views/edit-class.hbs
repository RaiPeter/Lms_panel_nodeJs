<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
    <title>Edit class</title>
</head>

<body>
    {{>navbar}}
    <main style="height:90vh" class="d-flex">
        {{>sidebar}}
        <div class="contentContainer w-100 p-3">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/">Home</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Edit Class</li>
                </ol>
            </nav>
            <h2>{{data.title}}</h2>
            
            <form action="/edit-class" method="post">
            <input type="hidden" name="class_id" value="{{data._id}}">
                <div class="row">
                    <div class="col-md-8 col-12">
                        <div class="mb-3">
                            <label for="title" class="form-label">Class No.</label>
                            <input type="number" name="class_no" id="title" class="form-control"
                                placeholder="Enter Course No" value="{{data.classNo}}" required>
                        </div>
                        <div class="mb-3">
                            <label for="title" class="form-label">Class Name</label>
                            <input type="text" name="class_title" id="title" class="form-control"
                                placeholder="Enter Course Title" value="{{data.classTitle}}" required>
                        </div>
                        <div class="mb-3">
                            <label for="desc" class="form-label">Class Description</label>
                            <textarea name="class_description" id="desc" class="form-control" cols="30" rows="5"
                                required>{{data.classDescription}}</textarea>
                        </div>
                        <div class="mb-3" >
                            <label for="thumbnail" class="form-label">Course video</label>
                            <video src="/classes/{{data.classFile}}" controls muted style="width: 100%; height: 170px"></video>
                        </div>
                      
                    </div>
                    <div class="col-md-4 col-12 pt-1">
                     
                        <button type="update" class="btn btn-primary mt-4">Update</button>
                        <a href="/delete-class?class={{data._id}}" class="btn btn-danger mt-4">Delete</a>
                    </div>
                </div>



            </form>

        </div>

        </div>

    </main>


    {{>footer}}

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="js/main.js"></script>
</body>

</html>